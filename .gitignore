/target
.slod/
*.slod.lock
# Legacy local trace data from before the traceframe -> slod rename.
.traceframe/

# Local-only agent instruction and run-state files (never tracked).
.claude/
.codex/
.omx/
.nahuali/
